{
  "name": "the-librarian-claude-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code plugin for The Librarian: memory + session lifecycle over a remote Librarian MCP server.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-bundle.mjs",
    "validate": "node scripts/validate.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
